[package]
name = "vhdesk-viewer"
description = "Aplicacion de escritorio del lado que controla: decodifica, renderiza y envia input."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
vhdesk-proto.workspace = true

anyhow.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
eframe.workspace = true
egui-wgpu.workspace = true

[dev-dependencies]
# Para el test de color: genera el I420 de referencia con la misma conversion que usa el
# host, de modo que el viaje de ida y vuelta (CPU -> GPU) quede probado de punta a punta.
vhdesk-codec.workspace = true
pollster.workspace = true
