[package]
name = "vhdesk-viewer"
description = "Aplicacion de escritorio del lado que controla: decodifica, renderiza y envia input."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
vhdesk-codec.workspace = true
vhdesk-input.workspace = true
vhdesk-proto.workspace = true
vhdesk-transport.workspace = true

anyhow.workspace = true
tokio.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true
eframe.workspace = true
egui-wgpu.workspace = true

# Solo para llegar a `with_msg_hook`, por donde el viewer ve los `WM_INPUT` de Raw Input.
egui-winit.workspace = true

[dev-dependencies]
# El test de color genera el I420 de referencia con la misma conversion que usa el host, de
# modo que el viaje de ida y vuelta (CPU -> GPU) quede probado de punta a punta.
pollster.workspace = true
